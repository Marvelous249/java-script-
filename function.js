function user(name, age){
	return `${name} is ${age} years`;
}

console.log(user("marvellous", 25));

const greet = function(param){
	return (`${param} to function expression`)
}

console.log(greet("good morning));